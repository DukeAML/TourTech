class DeepLog:
    # both will have their own lineParser... although, if deterministic, should have same results so can we duplicate it somehow? Inheritance in Python?
    def __init__(self):
        super().__init__()


class InvariantsMining:
    # both will have their own lineParser... although, if deterministic, should have same results so can we duplicate it somehow? Inheritance in Python?
    def __init__(self):
        super().__init__()
